import { Meta } from '@storybook/addon-docs';

<Meta title="Core/1. Introduction" />

# Core

The Core Library is the heart of **Konstruct-css**, and contain many of the base styles and variables used throughout the library.

## Installation

The simplest way to install this library:

```
import '@konstruct'
```
